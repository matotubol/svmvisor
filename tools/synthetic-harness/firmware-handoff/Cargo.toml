[package]
name = "svmvisor-firmware-handoff"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
uefi = { version = "=0.39.0", default-features = false }
uefi-raw = "=0.15.1"
svmvisor-hypervisor = { path = "../../../crates/hypervisor" }
