[package]
name = "xtask"
description = "Host-side build driver: `cargo xtask resident` builds and audits the resident payload and its DXE shim"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
sha2 = { version = "0.11", default-features = false }
