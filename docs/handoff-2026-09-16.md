# Current implementation handoff — 2026-09-16

## Latest delivery: compatibility fixes flashed, boot pending

Implementation commit `d9d07501330eb3456c79741db9168b0340a09da8` is pushed to
main and programmed as candidate `3a704487861c4b0792c93d16cfcb3a11`, using the
existing machine-specific `--boot --low-runtime` production profile.
Full 5 MiB readback SHA256:
`e11170c790a34cb190182a26fb1612efb008f4a6a5aac8cc32d05a5f0f2267c1`.
FPGA / ROM IDs: `25d93e5f5fde45d5` / `4fdd9c87e26ad519`.

Programming session `0b8c53a437d04e08a9a0ce0e4c92c664` retains two matching
5 MiB predecessor backups, both SHA256
`cd852f9192925f447d856cbe826c2b06ffa1a16ddde1b36d702bcd824a2c61fd`.
The session-bound restore helper passed its offline source check.
`D:/SVMvisor-Snapshot-Kit` now expects the new IDs; existing captures are preserved.
Replace any older remote copy before capture. No activation or Windows boot
was performed: full power-off/power-on is still required.

[Delivery evidence and recovery procedure](handoff-evidence/2026-09-16-compatibility-flash/)
bind the source commit, production audit, five packaged firmware fixtures,
FPGA review, programmer and independent readback hashes. Host suites passed
565 hypervisor, 28 boot-library and 302 returning tests. The production audit
covers 24,981 instructions. Static FPGA checks do not establish native timing.

## Included review changes and remaining gaps

The [second compatibility review](native-boot-compatibility-review-2026-09-16.md)
adds CPUID NRIPS mode/prefix handling, coherent HWCR35 fault semantics, scalar
CF8–CFF I/O forwarding and all-excluding-self startup routing. Local tests and
the production audit pass. The separate NRIPS emulator build now passes actual
compatibility CPUID, prefixed MSR, cache and restart fixtures; see the linked
review for exact hashes and evidence limits. Interrupted-event replay, active-LAPIC INIT and general MSR
mode/prefix completion remain unresolved. These reviewed changes are included
in the newly flashed candidate above.

See [the boot exit review](native-boot-trap-review-2026-09-16.md). Silent terminal
returns now record reasons, and unresolved interrupted delivery cannot pass
through native retry paths unnoticed. General event reinjection remains
unsupported. Tests and a new production audit pass. ECAM.US was verified correct;
the 1 TiB aperture is a real limit
without a demonstrated current-machine resource conflict.

## Previous delivery: HWCR fix

The CPU-local HWCR bit-30 policy fix and additional refusal diagnostics are now
implemented directly on main. Manual verification is recorded in
[native-hwcr-manual-verification.md](native-hwcr-manual-verification.md).
The new candidate `bfb3f06466d24a24a53e121a594ec7a3` was programmed successfully;
full 5 MiB readback SHA256 is
`cd852f9192925f447d856cbe826c2b06ffa1a16ddde1b36d702bcd824a2c61fd`.
FPGA / ROM IDs: `3f6d1a7188aa62de` / `aa8cfc3b67bf11f0`.
Two full predecessor backups independently match the historical image below.
No activation or new physical boot has been performed. Full power-off/power-on
is required; Windows boot remains unproven.

Programming session: `d8632a78a5cb4050a6cc53b9d6090e2e`, under
`target/firmware/squirrel/card-resident-reviewed-sessions/` in the main workspace.
The restore source passed offline validation. `D:/SVMvisor-Snapshot-Kit` now
expects the new IDs and passed 71 offline tests. Replace any remote copy of the
kit before capturing the next boot.

Validation: 555 hypervisor tests, 28 DXE boot-library tests, 302 returning tests,
positive and refusal HWCR fixtures, five packaged card fixtures, and the
23,548-instruction production audit passed. Fixture HWCR accesses use a modeled
backend; these results do not establish physical HWCR behavior or Windows boot.
See [portable validation and flash evidence](handoff-evidence/2026-09-16-hwcr/).
Its `validation.json` records the earlier pre-flash checkpoint;
`flash-result.json` supersedes that checkpoint's delivery status.

## Historical imported checkpoint and last physical boot

This checkpoint comes from `C:/Users/mato/.codex/worktrees/7a58/svmvisor`.
It replaces the older local main source; older main edits are preserved separately.
The latest physical boot still hangs. Windows under the hypervisor is not yet working.
Older dated reports describe historical milestones, not the current result.

## Architecture and latest correction

Firmware initializes hardware normally. A PCI option-ROM DXE driver prepares
resident ownership. After the original ExitBootServices succeeds, the monitor
activates AMD SVM on all CPUs and returns the Windows loader into the guest.
The latest correction captures and validates physical cache banks after firmware's
BSP-to-AP synchronization, then checks exact state again when arming each CPU.
It does not normalize physical MTRRs or mask away inconsistent routing bits.

## Exact physical evidence

Platform: Ryzen 9 9900X, Gigabyte B850 AORUS ELITE WIFI7, BIOS F7, Windows 11.
Flashed candidate: `34f1d747f8cb47689ba4a06a8646c621`.
Combined image SHA256: `903eb3f397cbd34390bec6e250490fa815de029c5c821cdf07cdf44e4ec9510b`.
FPGA / ROM IDs: `27ddef70674522cc` / `f2e3abb0b289c024`.
Snapshot: `a6502f145ed84316b5d34cf848e07d03`, boot ID `1875948883`.

All 24 CPUs activated before loader return (stage 5). This passes the prior
post-firmware admission failure. BSP first fault: bank 32, sequence 47302,
RIP `FFFFF8077DB5FD56`, exit `0x7C`, reason `0xF400`, detail `0x10`,
exit count 26051. The terminal barrier did not complete.

`cache_runtime.rs` maps detail 16 to an ordinary cache-owner write returning
`CacheWriteError::Unsupported`. The record does not contain RCX, the requested
value, or the owner phase. Do not claim that the snapshot alone identifies an MSR.

## Strong next lead: retained kernel HWCR write

Binary Ninja 6 scanned every matching RVA offset across possible 64 KiB-aligned
kernel bases and found exactly one WRMSR candidate in the retained kernel:
preferred VA `0x140B5FD56`, RVA `0xB5FD56`, function `sub_140B5F8F8`.
The inferred running kernel base is `FFFFF8077D000000`.
The instruction sequence reads MSR `0xC0010015` (HWCR), sets bit 30, and writes it.
The cache owner in that historical image rejects changed HWCR values, which fits the refusal.

This is strong static attribution, not verified runtime operands or module identity.
Retained kernel: 10.0.26100.9444, SHA256
`75665496a75fc020a0573523f3f77d7c719479aceb5806a98d82ff2d1b5314a1`.
Review the original applicable AMD HWCR bit-30 rules and ownership implications
before implementing a change. No HWCR fix had been implemented at that checkpoint;
the latest update above supersedes this implementation status.
The earlier source investigation note predates this additional static lead.

## Start here

- `crates/hypervisor/src/svm/native_cache.rs`: logical cache/MSR ownership.
- `crates/hypervisor/src/host/resident/cache_runtime.rs`: write/refusal handling.
- `crates/dxe/src/native/resident/physical_boot.rs`: post-firmware survey.
- `crates/dxe/src/native/resident/activation.rs`: activation and drift checks.
- `firmware/squirrel/read_snapshot.py`: snapshot transport/decoder.
- `docs/native-cache-replay-owner.md`: ownership contract.
- [Portable capture, static analysis and build evidence](handoff-evidence/2026-09-16/).

## Validation and local artifacts

Existing validation reports record 552 hypervisor tests, 28 DXE boot-library tests,
302 DXE native-returning tests, positive/mismatch/drift four-CPU survey fixtures,
and five packaged card fixtures passing. Linked audit covered 23,384 integer
instructions with no FP/SIMD/xstate or undefined instructions. Physical flash
readback matched. These checks do not establish Windows boot success.
The full DXE native-resident-boot host-test attempt encountered a feature-gating
mismatch; do not describe every possible test profile as passing.

Build entry point: `python tools/native-resident/build.py --output work/NEW --boot --low-runtime`.
The current commit adds a handoff, not a newly built or flashed candidate.
Large scratch research, downloaded tools, BN databases and compiled images remain
locally in the original worktree under `work/` and `target/`, excluded from Git.
Selected portable evidence is committed; historical reports may reference other
local files. Original full evidence: `work/native-fixed-mtrr-2026-09-15` and
`work/native-cache-write-2026-09-16`.

## Working location for the fresh task

User instruction: work directly on main in C:/Users/mato/Documents/svmvisor.
Do not create or use worktrees for implementation. The old 7a58 directory is a
read-only archive for large local evidence and analysis databases.

